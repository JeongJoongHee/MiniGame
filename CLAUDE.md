# 심심할 때 하는 게임 — 세로형 미니게임 모음

`D:\00.JumpJump` · Unity 6000.0.81f1 · URP 2D · Input System
1인 개발(zkfpf). 기획서와 손스케치를 기반으로 2026-09-05에 구현.

**앞으로 여기에 미니게임을 하나씩 추가하는 형태로 개발한다.**
2026-09-06에 타이틀 화면 + 로비(게임 선택) 껍데기를 붙였고,
첫 번째 미니게임이 "점프점프!"(JumpJump), 두 번째가 "활쏘기!"(Archery)다.
2026-09-08에 **설정 / 나가기 팝업**을 붙여 앱을 나가는 길이 전부 확인 팝업을 거치게 되었다.

```
타이틀(TitleScene) --START--> 로비(LobbyScene) --PLAY--> 미니게임 씬
                                  ^                        |
                                  +------ <- (확인 팝업) ---+
```

| 폴더 | 내용 |
| --- | --- |
| `Assets/Shell/` | 껍데기 — 타이틀 / 로비 / 게임 목록. 네임스페이스 `Arcade` |
| `Assets/JumpJump/` | 미니게임 1번 "점프점프!". 네임스페이스 `JumpJump` |
| `Assets/Archery/` | 미니게임 2번 "활쏘기!". 네임스페이스 `Archery` (2026-09-07 추가) |

**미니게임 추가 방법은 `JumpJump/Assets/Shell/README.md` 에 있다.**
요약하면 씬 만들기 → 작업 폴더에 `@Game_NN_이름.png`(아이콘)과
`@Name_NN_이름.png`(이름표) 넣기 → Build All Scenes →
`GameCatalog.asset` 에 한 줄 추가. **한 군데만 코드를 고쳐야 하는데**,
새 씬을 `ShellSceneBuilder` 의 씬 목록(`RegisterBuildSettings` / `BuildAll`)과
`JumpJumpBuild.Scenes`(APK 용)에 넣는 것이다. 활쏘기를 붙일 때 고친 곳이 딱 거기다.

---

## 이어서 시작하기

**2026-09-08 (4번째 세션) 종료 시점 상태 (마지막 작업일):**

- **`@수정사항_02.pdf` 3건을 반영했다 — 설정 버튼 / 나가기 팝업 / 폰 뒤로 버튼.**
  아래 "설정 · 나가기 팝업" 절과 `JumpJump/Assets/Shell/README.md` 의 같은 절.
  사용자가 그림 9장을 `@리소스 추가_1차` 폴더로 넣어 주었고 전부 그대로 썼다.
- **버그 하나를 같이 고쳤다 — 미니게임 HUD 캔버스에 `GraphicRaycaster` 가 없었다.**
  즉 **3번째 세션에 붙인 "< LOBBY" 버튼은 눌러도 반응하지 않는 상태였다.**
  배치 모드로는 "보이는지"까지만 확인되고 "눌리는지"는 확인할 수 없어서 놓쳤던 것이다.
  이제 `MakeExitToLobbyUI` 가 없으면 붙여 준다. **앞으로 같은 일은 생기지 않는다.**
- 배치 모드 6종 전부 통과. 컴파일 오류 0건, 오류 로그 0건.
  점프점프 166칸 / 47900점, 활쏘기 조준 봇 96발 명중, 빗맞히는 봇 7판 전부 OUT OF ARROWS.
  **팝업도 배치 모드에서 켜서 찍도록 만들어 두었다** (`00_popup_*.png` / `04_exit_popup.png`).
- **사용자가 3세션치를 직접 플레이해 보고 수정사항_02 를 가져왔다.**
  4번째 세션 시작 때 "지난 작업 중 더 고칠 곳"을 물었더니 **"괜찮다, 이번 것만"** 이라고 했다.
  즉 **활쏘기 / 점프점프 1차 수정 / 화면 수정 4건은 사용자 확인이 끝난 것으로 본다.**
- **이번 팝업 작업은 아직 미확인이다.** 재개하면 먼저 플레이해 봐 달라고 할 것.

**그 이전 (2026-09-07, 3번째 세션):**

- **두 번째 미니게임 "활쏘기!"를 추가했다.** `@활쏘기 기획서.png` 기반.
  난이도는 `archery.csv` 구간표로 굴러간다.
  자세한 내용은 아래 "활쏘기" 절과 `JumpJump/Assets/Archery/README.md`.
- **그날 오후 사용자가 활쏘기 그림(과녁·활·화살)과 로비 아이콘·이름표를 직접 넣었다.**
  임시 그림은 이제 안 쓴다. 그림 교체로 크기가 깨지지 않도록 `ArcheryArtSetup` 을 붙였다
  (여백 자르고 PPU·피벗 역산). 아래 "그림을 교체해도 크기가 유지된다" 절.
- **화면 4건을 손봤다** — 타이틀 이름 크게 / START 0.7배 / 로비 이름표 크기 통일 /
  게임마다 오른쪽 위 "< LOBBY" 버튼. 아래 "화면 수정 (3번째 세션)" 절.
- 점프점프는 플레이 가능한 완성 상태. 난이도는 `config.csv` 구간표로 굴러간다.
- 타이틀 + 로비 껍데기가 붙어 "미니게임 모음 앱"이 되었다 (2026-09-06).
  씬 **4장**(TitleScene / LobbyScene / GameScene / ArcheryScene)이 전부 구워져 있고
  Build Settings 순서도 맞다. 로비의 2번 칸이 활쏘기로 열려 있다.
- 배경은 "높이별 3장 + 크로스페이드" 다. 0m 평야 / 200m 산 / 500m 우주.
  자세한 내용은 아래 "높이별 배경" 절.
- **사용자가 직접 플레이해 보고 `@1차 수정 리포트.pdf` 로 5건을 가져왔고 그날 전부 반영했다.**
  START 버튼 위치 / 타이틀 이름을 그림으로 / 이름표를 그림으로 / 톱니바퀴 제거 /
  화면 밖 발판에 착지해 살아남던 버그. 자세한 내용은 아래 "1차 수정 리포트" 절.
- 배치 모드 검증 전부 통과 — 컴파일 오류 0건, 미리보기 13장 생성,
  자동 플레이테스트 정상, 발판 버그는 재현 + 수정을 코드로 직접 확인했다.
- 세션 끝에 배치 모드 6종을 연달아 돌려 전부 통과했다.
  (이 세션 작업은 2026-09-08 에 사용자 확인이 끝났다)

즉 **지금 코드는 "검증까지 끝난 안정 상태"** 다. 재개 시점에 뭔가 깨져 있다면
내가 아니라 그 사이의 변경 때문이니, 먼저 검증 명령부터 돌려서 현재 상태를 확인할 것.

> **에디터를 열었을 때 빈 씬이 떠 있어도 정상이다.** 세션 마지막에 돌린 배치 플레이테스트가
> 임시 빈 씬을 만들어 두고 끝나기 때문이다. `Tools > Arcade > Build All Scenes` 를 한 번 누르면
> 씬 4장이 다시 구워지고 **TitleScene 이 열린 상태**가 되어 Play 로 앱 처음부터 돌 수 있다.

재개하면 이 순서로:

1. 이 문서의 **"대기 중 / 미결정"** 섹션을 읽는다.
   **맨 위 "0번" 이 지난 세션이 끊긴 지점이다. 거기부터 물어볼 것.**
2. 작업 폴더에 `수정사항_NN.xlsx/.pdf` 나 새 기획서(`*_기획서_NN.pptx/.pdf`)가
   새로 생겼는지 확인한다. 이 사용자는 수정 목록을 그런 파일로 가져온다.
3. 사용자가 가져온 수정 목록을 받는다.
4. 수정 후 반드시 **아래 "검증 방법"** 을 돌려서 컴파일·동작·화면을 직접 확인하고 보고한다.
   (이 프로젝트는 매번 배치 모드로 실제 검증한 뒤 보고하는 방식으로 진행해 왔다)

### 지금까지의 보고서 (작업 폴더)

| 파일 | 내용 |
| --- | --- |
| `수정사항_01_반영보고서.html` | 1차 수정 반영 (난이도 구간표 도입) |
| `config_csv_적용보고서.html` | 난이도 구간표를 CSV 로 분리 |
| `타이틀_로비_적용보고서.html` | 타이틀 + 로비 껍데기 추가 |
| `배경이미지_준비가이드.html` | 배경 그림을 새로 뽑을 때의 조건 (판형·이음매·금지 요소) |
| `배경타일_시험결과.html` | 원본 3장에서 반복 가능한 타일을 뽑아낸 결과 |
| `배경_구간전환_적용보고서.html` | 높이별 배경 3장 + 크로스페이드 적용 |
| `1차수정리포트_반영보고서.html` | `@1차 수정 리포트.pdf` 5건 반영 |
| `활쏘기_추가보고서.html` | 미니게임 2번 "활쏘기" 추가 |
| `화면수정_반영보고서.html` | 화면 수정 4건 + 그림 교체 대응 |
| `설정팝업_반영보고서.html` | **설정 버튼 + 나가기 팝업 + 폰 뒤로 버튼 (가장 최신)** |

### 작업 폴더의 APK — 한 세대 뒤처져 있다

`D:\00.JumpJump\JumpJump.apk` 는 **2026-09-07 17:02 빌드**다 (46.9MB).
사용자가 3번째 세션 끝에 에디터에서 직접 구웠다.
타이틀 / 로비 / 점프점프 / 활쏘기 4장과 화면 수정 4건까지는 들어 있지만,
**2026-09-08 의 설정·나가기 팝업은 들어 있지 않다.** 폰에서 확인하려면 다시 구워야 한다.

다시 구우려면 **Tools > JumpJump > Build Android APK**.
IL2CPP 라 10분 이상 걸리므로 배치로 돌릴 때는 백그라운드로 띄울 것.

상세 문서는 세 개다. **코드를 읽기 전에 먼저 읽을 것.**

| 문서 | 내용 |
| --- | --- |
| `JumpJump/Assets/Shell/README.md` | 타이틀/로비, **미니게임 추가 방법**, 화면 비율 처리 |
| `JumpJump/Assets/JumpJump/README.md` | 점프점프 규칙표·튜닝·아트 파이프라인·설계 메모 |
| `JumpJump/Assets/Archery/README.md` | 활쏘기 규칙표·`archery.csv` 열 설명·임시 그림 교체 방법 |

---

## 실행 방법

Unity 에디터에서 **Tools > Arcade > Build All Scenes** → **Play**.
타이틀 / 로비 / 미니게임 씬 4장이 통째로 구워지고 Build Settings 순서까지 정리된다.
마지막에 TitleScene 이 열려 있으므로 Play 를 누르면 앱 처음부터 돈다.

조작은 두 게임 다 **화면 터치 / 마우스 좌클릭 / 스페이스바** 하나뿐이다.
점프점프는 수직 점프, 활쏘기는 화살 발사.

---

## 검증 방법 (중요)

이 프로젝트는 **플레이 모드에 들어가지 않고 배치 모드로 전부 검증**할 수 있게 만들어 두었다.
각 게임의 `Step(dt, tap)` 이 입력과 델타타임을 인자로 받기 때문에 실제 게임 코드를
그대로 재생할 수 있다. (`JumpJump.GameManager.Step` / `Archery.ArcheryGame.Step`)
**미니게임을 새로 만들 때도 이 구조를 지킬 것.** 이게 이 프로젝트의 검증 방식 전부다.

```bash
UNITY="C:/Program Files/Unity Hub/6000.0.81f1/Editor/Unity.exe"
PROJ="D:/00.JumpJump/JumpJump"

# 1) 컴파일 + 씬 4장 생성 (타이틀 / 로비 / 점프점프 / 활쏘기)
"$UNITY" -batchmode -quit -projectPath "$PROJ" \
  -executeMethod Arcade.EditorTools.ShellSceneBuilder.BuildAll -logFile <로그>

# 1b) 타이틀 / 로비 화면 PNG (9:16 과 20:9 두 벌) -> JumpJump_Preview/
"$UNITY" -batchmode -quit -projectPath "$PROJ" \
  -executeMethod Arcade.EditorTools.ShellPreview.Capture -logFile <로그>

# 2) 점프점프 자동 플레이테스트 (120초 시뮬레이션, 봇이 몇 칸까지 오르는지)
"$UNITY" -batchmode -quit -projectPath "$PROJ" \
  -executeMethod JumpJump.EditorTools.JumpJumpPlaytest.Run -logFile <로그>

# 3) 점프점프 화면 PNG 캡처 -> D:\00.JumpJump\JumpJump\JumpJump_Preview\
"$UNITY" -batchmode -quit -projectPath "$PROJ" \
  -executeMethod JumpJump.EditorTools.JumpJumpPreview.Capture -logFile <로그>

# 2b) 활쏘기 자동 플레이테스트 (조준 봇 120초 + 일부러 빗맞히는 봇 30초)
"$UNITY" -batchmode -quit -projectPath "$PROJ" \
  -executeMethod Archery.EditorTools.ArcheryPlaytest.Run -logFile <로그>

# 3b) 활쏘기 화면 PNG 캡처 (10_~13_archery_*.png)
"$UNITY" -batchmode -quit -projectPath "$PROJ" \
  -executeMethod Archery.EditorTools.ArcheryPreview.Capture -logFile <로그>
```

```bash
# 4) 안드로이드 APK -> D:\00.JumpJump\JumpJump.apk
"$UNITY" -batchmode -quit -projectPath "$PROJ" -buildTarget Android \
  -executeMethod JumpJump.EditorTools.JumpJumpBuild.BuildApk -logFile <로그>
```

에디터 안에서는 같은 기능이 **Tools > Arcade** (껍데기) / **Tools > JumpJump** (점프점프) /
**Tools > Archery** (활쏘기) 메뉴에 나뉘어 있다.
평소에는 `Tools > Arcade > Build All Scenes` 하나면 된다.

APK 는 씬 4장을 모두 담고, ARM64 / IL2CPP / 세로 고정 / 패키지 `com.zkfpf.jumpjump` 이고
**Unity 디버그 키로 서명**되므로 사이드로드 전용이다. 스토어 업로드를 하게 되면
`JumpJumpBuild.cs` 의 `useCustomKeystore`(서명)와 `buildAppBundle`(AAB) 을 바꿔야 한다.
첫 IL2CPP 빌드는 10분 이상 걸리니 배치로 돌릴 때는 백그라운드로 띄울 것.

### 걸리는 점

- **Unity 에디터가 열려 있으면 배치 모드가 실패한다** (프로젝트 락).
  먼저 `Get-Process Unity` 로 확인하고, 켜져 있으면 사용자에게 닫아 달라고 요청할 것.
  절대 강제 종료하지 말 것 — 저장 안 된 작업이 날아간다.
- 배치 모드의 첫 `cam.Render()` 는 스프라이트 머티리얼과 동적 폰트 아틀라스가
  준비되기 전이라 화면이 엉뚱하게 나온다. `JumpJumpPreview` 는 두 번 렌더해서 우회한다.
- 배치 모드에서는 `Screen` 크기가 더미라 CanvasScaler 가 잘못 계산한다.
  `JumpJumpPreview` 는 ConstantPixelSize 로 강제해서 실제 레이아웃과 맞춘다.

---

## 확정된 규칙 (사용자가 결정함)

기획서에 미정으로 남아 있던 항목을 2026-09-05에 다음과 같이 확정했다.

| 항목 | 규칙 |
| --- | --- |
| 타이머 | 초당 1 감소. **새 칸을 밟으면 그 높이 구간의 값으로 초기화**. 구간별 5초 -> 1초 |
| 점수 | **도달 높이 + 콤보 배수**. 새 칸당 `100 x (1 + 콤보 x 0.1)`, 최대 3배 |
| 낙하 | 카메라는 위로만 이동. **화면 아래로 벗어나면 즉시 게임 오버** |
| 캐릭터 | 판 시작 시 5종 중 **랜덤 1개** |
| 발판 | `Base.png` 한 장 = 한 블록, 옆으로 타일링 |
| 배경 | 높이별 3장(`BG_01~03.png`)을 세로 무한 반복. 구간 경계에서 크로스페이드 |
| 타이틀 이름 | 글자가 아니라 그림 `@Title_Text.png` (2026-09-07) |
| 로비 이름표 | 게임마다 따로. 이름이 그려진 그림 `@Name_NN_이름.png` (2026-09-07) |
| 나가기 | 앱을 나가는 길은 전부 **확인 팝업 한 번**을 거친다 (2026-09-08) |
| 폰 뒤로 버튼 | 팝업이 떠 있으면 그것부터 닫기. 미니게임=로비 확인, 타이틀·로비=종료 확인 |

이미 사용자가 결정한 사항이므로 다시 묻지 말 것.

### 난이도 구간표는 `config.csv` 가 원본이다 (2026-09-06)

높이 구간별 **발판 최소~최대 블록 수 / 타이머 / 이동 속도**를 CSV 한 장으로 관리한다.
발판 폭은 구간의 최소~최대 사이에서 **칸마다 무작위**로 뽑는다.

- 사용자가 고치는 파일: `D:\00.JumpJump\config.csv`
- 게임이 읽는 사본: `JumpJump/Assets/JumpJump/config.csv` (TextAsset)
- 바깥 파일이 더 새로우면 `JumpJumpConfigCsv.Sync()` 가 자동 복사한다.
  `JumpJumpSceneBuilder.LoadOrCreateConfig()` 에서 호출되므로
  BuildScene / Playtest / Preview 를 돌리면 항상 최신 CSV 가 반영된다.
- 파서는 `Scripts/DifficultyTable.cs`. **열 이름으로** 찾으므로 열 순서 변경·
  추가 열·오타(`Heigh_Max`)·BOM·CRLF 를 전부 허용한다.
- `GameConfig.difficultyBands` 는 **읽은 결과를 구워 둔 거울**이다. 원본이 아니다.
  수치를 바꿔 달라는 요청이 오면 **CSV 를 고칠 것.**

표 전문과 열 설명은 `JumpJump/Assets/JumpJump/README.md` 의 "난이도 구간표" 절.

기존의 연속 보간 방식(`startBlocks`/`maxSpeed`/`blocksMinAtRow` 등)과
누적 회복식 타이머(`maxTime`/`timeGainPerRow`)는 이때 전부 제거했다.

### 높이별 배경 (2026-09-07)

배경은 **높이에 따라 3장이 서서히 갈아탄다.** 구간 경계 앞 30m 에 걸쳐 겹쳐 넘어간다.

| 그림 | 구간 | 내용 |
| --- | --- | --- |
| `Art/Background/BG_01.png` | 0m ~ | 평야 하늘 + 구름 |
| `Art/Background/BG_02.png` | 200m ~ | 높은 하늘 + 산맥 |
| `Art/Background/BG_03.png` | 500m ~ | 우주 + 별밭 + 성운 |

- 세 장 모두 **896x1400** 이고 **위끝과 아래끝이 이어지도록** 만들어져 있다.
  그래서 세로로 무한 반복해도 이음매가 보이지 않는다. **그림을 교체할 때 이 조건이 가장 중요하다.**
  조건 전문은 작업 폴더의 `배경이미지_준비가이드.html`.
- **전환 높이는 `GameConfig.asset` 의 `backdropBands`**, 겹치는 길이는 `backdropBlendMeters`(기본 30m).
  수치를 바꿔 달라는 요청이 오면 코드가 아니라 여기를 고칠 것.
- **그림을 추가할 때 코드를 고치지 말 것.** `Art/Background/` 에 `BG_04.png` 를 넣고
  Build Game Scene 을 돌리면 `JumpJumpArtSetup.BackgroundPaths()` 가 폴더를 훑어서 목록에 붙인다.
  다만 **장수가 바뀌면 전환 높이가 기본값(0/200/500/1000...)으로 다시 잡히므로** 그 뒤에
  인스펙터에서 원하는 값으로 고쳐야 한다. 장수가 그대로면 손으로 고친 높이는 유지된다.
- 구현은 `BackdropTiler` 가 배경을 **두 겹(Current / Next)** 으로 깔고 위 겹의 투명도를 올리는 방식.
  `GameManager` 가 매 프레임 `backdrop.Tick(현재 높이 m)` 를 부른다.
- **원본 가로형 3장은 `D:\00.JumpJump\BG_원본\` 에 백업해 두었다.** (구 `BG.jpeg` 포함)
  지금 쓰는 타일은 그 3장에서 구름·산맥·별밭을 뽑아 만든 것이고,
  만든 스크립트는 `D:\00.JumpJump\BG_시험\배경타일_만들기.py` 다.
  **세로형 그림을 새로 받으면 그 스크립트로 이음매만 맞춰서 다시 뽑으면 된다.**

### 1차 수정 리포트 반영 (2026-09-07)

`@1차 수정 리포트.pdf` 5건을 그날 전부 반영했다.

| # | 요청 | 어디를 고쳤나 |
| --- | --- | --- |
| 1 | START 버튼 top 106 / bottom -106 | `TitleScreen.startCenter` y 0.251 -> 0.196 (아래로 106px) |
| 2 | 타이틀 이름을 `@Title_Text.png` 그림으로 | `TitleScreen.titleImage`. 글자 UI 는 없앴다 |
| 3 | 로비 이름표를 글자 없는 그림으로 | `MiniGameEntry.namePlate` 추가. 게임마다 따로 |
| 4 | 로비 톱니바퀴 제거 | `@Lobby.png` 그림 자체를 수정 |
| 5 | 화면 밖 발판에 착지해 살아남던 버그 | `PlatformManager.FindLanding` 에 `minSurfaceY` 추가 |

**1번 "top 106 / bottom -106"** 은 Unity 인스펙터의 Top/Bottom 값으로 읽어서
"크기 그대로 아래로 106px" 로 해석했다. 사용자가 확인해 준 해석이다.
1080x1920 기준 106px = 배경 높이의 약 5.5%.

**2번 타이틀 이름 그림**의 크기는 `TitleScreen.titleWidth`(기본 0.72 = 배경 폭의 72%),
위치는 `titleCenter`. 인스펙터에서 고치면 된다. 코드 상수가 아니다.

**3번 이름표**는 이제 게임마다 다른 그림이다. 게임 이름이 그림 안에 그려져 있어서
코드가 글자를 따로 얹지 않는다. 작업 폴더에 `@Name_NN_이름.png` 를 넣으면
아이콘(`@Game_NN_*.png`)과 똑같이 자동으로 들어와 카탈로그에 순서대로 붙는다.
**게임을 추가할 때 코드를 고칠 필요는 여전히 없다.** 그림이 한 장 늘었을 뿐이다.

**4번 톱니바퀴**는 코드가 아니라 그림에 그려져 있었다. 오른쪽 위 자리를 바로 옆 숲을
좌우로 접어 덮는 방식으로 지웠다. **톱니바퀴가 있는 원본은
`D:\00.JumpJump\Lobby_원본_톱니바퀴포함.png` 로 남겨 두었다.** 되돌리려면 그 파일을
`@Lobby.png` 로 덮어쓰고 Build All Scenes 를 돌리면 된다.
나중에 설정 화면을 진짜로 만들 때는 톱니바퀴를 그림이 아니라 UI 버튼으로 얹는 편이 낫다.

**5번 발판 버그**의 원인은 이랬다. 발판은 화면 아래로 사라진 뒤에도 재활용될 때까지
카메라 아래 4유닛까지 살아 있는데, 낙사 판정선(화면 아래끝 - `deathMargin`)이 그보다
아래에 있었다. 그래서 화면 밖으로 떨어져도 **보이지 않는 발판에 착지해 살아남는** 일이
생겼다. 이제 `FindLanding` 이 화면 아래끝보다 낮은 발판을 아예 후보에서 뺀다.
(검증: 20칸까지 올라간 시점에 화면 밖 발판이 실제로 2개 살아 있었고,
고치기 전 규칙으로는 착지, 고친 뒤 규칙으로는 통과하는 것을 배치 모드로 확인했다)

### 미니게임 2번 "활쏘기!" (2026-09-07, 3번째 세션)

`@활쏘기 기획서.png`(PRECISION ARCHER) + 손스케치 `Game_02.png` 를 그대로 구현했다.
**규칙표·CSV 열 설명·그림 교체 방법은 `JumpJump/Assets/Archery/README.md` 에 있다.**

| 항목 | 규칙 |
| --- | --- |
| 조작 | 터치 / 좌클릭 / 스페이스바 = **화살 발사** 하나뿐 |
| 활 | 화면 아래 가운데 **고정**. 화살은 항상 수직으로 날아간다 |
| 과녁 | 화면 위에서 좌우 왕복. 벽에 닿으면 반사 |
| 점수 | 링 5개. **가운데 5점, 바깥으로 갈수록 1점씩 줄어 가장자리 1점** |
| 화살 | 5발. 쏘면 1 줄고 **맞히면 최대치로 가득 찬다** |
| 과녁 크기 | 맞힐 때마다 **1% 씩 작아진다** (하한 35%) |
| 게임 오버 | **화살이 다 떨어지면** (= 연속 5발 빗나가면) |

- **난이도 원본은 `D:\00.JumpJump\archery.csv`** 다. 점프점프의 `config.csv` 와 같은 방식이고,
  구간을 가르는 기준만 다르다 — 점프점프는 높이(m), 활쏘기는 **누적 명중 수**다.
  열: `Hits_Max` / `Target_Speed` / `Shrink_Percent` / `Size_Min_Percent` / `Ammo_Max` / `Arrow_Speed`.
  **수치를 바꿔 달라는 요청이 오면 코드가 아니라 이 CSV 를 고칠 것.**
  `ArcheryConfig.stageBands` 는 읽은 결과를 구워 둔 거울이다.
  **사용자가 "테이블이 추가될 예정"이라고 했으므로, 열을 늘리기 쉽게 만들어 두었다.**
  파서(`StageTable.cs`)는 열을 이름으로 찾고, 빈 칸은 위 줄 값을 이어받고,
  모르는 열은 무시한다. 열 추가 절차는 Archery README 의 "나중에 열을 추가할 때" 절.
- **그림은 사용자가 넣은 진짜 아트다** (`Assets/Archery/Art/` 의 Target / Arrow / Bow).
  덮어쓰기만 하면 되고 **픽셀 크기·여백이 얼마든 상관없다** — 아래 절 참고.
  단 **과녁 그림은 원이 한가운데** 있어야 한다. 과녁의 x 위치가 곧 명중 판정이라
  치우쳐 있으면 보이는 곳과 맞는 곳이 달라진다.
- 로비 아이콘·이름표도 사용자가 `Shell/Art/Games` · `Shell/Art/Names` 에 직접 넣었다.
  그래서 **앱에서 한글을 글자로 그리는 곳이 이제 없다.**
- 명중 판정은 **x 거리 하나**로 끝난다. 화살이 수직으로만 날기 때문에,
  촉이 과녁 높이를 통과하는 순간의 `|화살x - 과녁x|` 를 반지름으로 나누면 링이 정해진다.
  점프점프와 마찬가지로 **물리 엔진을 쓰지 않는다.**
- 화살이 0발이어도 **날아가는 중인 화살이 있으면 게임 오버를 미룬다.**
  마지막 한 발이 맞으면 화살이 다시 차기 때문이다 (`ArrowPool.PendingCount`).

### 그림을 교체해도 크기가 유지된다 (2026-09-07)

스프라이트의 세계 크기는 `픽셀 수 ÷ PPU` 다. PPU 를 고정해 두면 320px 짜리 임시 그림 자리에
2400px 짜리 진짜 그림을 넣는 순간 활이 화면을 뒤덮는다. **실제로 그렇게 됐고**, 그래서
`ArcheryArtSetup` 을 붙였다. 점프점프의 `JumpJumpArtSetup` 과 같은 방식이다.

- **여백을 잘라 내고, 원하는 세계 크기에서 PPU 를 역산한다.**
  화살은 세로 = `arrowLength`, 활은 가로 = `bowWidth`, 과녁은 피벗만 한가운데로.
- 계산에 쓴 값을 `userData` 에 적어 두므로 **그림을 갈아 끼우면 다음 빌드에서 알아서 다시 잡는다.**
  (점프점프는 메뉴를 손으로 눌러야 하는 것과 다르다)
- **활의 피벗은 아래끝**이라 `bowY` 는 활의 "밑동 높이"다. 중심이 아니다.
- 껍데기 쪽도 같이 손봤다. `Art/Games` · `Art/Names` 에 **작업 폴더를 거치지 않고 바로 넣은
  그림도 여백을 잘라 낸다** (`ShellArt.TrimStrayArt`). 안 자르면 그림마다 여백이 달라서
  로비 아이콘·이름표 크기가 들쭉날쭉해진다. 실제로 그 문제로 이름표 크기가 어긋났었다.

### 화면 수정 (2026-09-07, 3번째 세션)

사용자가 4건을 요청해 반영했다.

| # | 요청 | 어떻게 |
| --- | --- | --- |
| 1 | 타이틀 제목 2배 | `TitleScreen.titleWidth` 0.72 -> **0.94, 기준을 배경에서 화면으로 바꿈** |
| 2 | START 0.7배 | `TitleScreen.startWidth` 0.459 -> 0.321 |
| 3 | 로비 이름표 크기 통일 | `LobbyScreen` 에 `plateHeight` 추가, **그림 비율 대신 고정 칸 크기** 사용 |
| 4 | 게임마다 뒤로가기 버튼 | `ShellSceneBuilder.MakeLobbyButton` — 두 게임 **오른쪽 맨 위, 항상 표시**<br>(2026-09-08 에 `MakeExitToLobbyUI` 로 바뀌었다) |

**1번은 "2배"를 그대로 하지 못했다.** 제목 그림은 가로로 긴 한 줄짜리(2192x534)라,
배경 폭의 72%가 이미 **요즘 폰 화면의 90%** 였다. 2배로 잡으면 첫 글자와 끝 글자가 잘린다.
그래서 **화면 폭의 94%(= 잘리지 않는 최대치)** 로 했고, 사용자에게 그렇게 보고했다.
진짜로 2배가 필요하면 **제목 그림을 두 줄로 다시 그려야 한다.**

이때 기준도 바꿨다. 타이틀 배경은 Cover(넘치는 쪽을 자름)라 20:9 폰에서 좌우가 20% 잘린다.
제목을 배경 기준으로 두면 같이 잘리므로 **제목만 캔버스(화면)에 붙였다.**
높이는 계산하지 않고 `preserveAspect` 에 맡긴다 — 화면 비율을 직접 읽어 계산했더니
그 값이 준비되기 전인 순간에 글자가 찌그러졌다.

**3번의 진짜 원인**은 이름표 그림의 여백이었다. 사용자가 `Name_02` 를 작업 폴더가 아니라
프로젝트에 직접 넣어서 여백(2400x1308 중 실제 그림 1797x838)이 안 잘렸고,
같은 칸에 그려도 그림이 작아 보였다. 고정 칸 크기 + `TrimStrayArt` 로 둘 다 해결했다.

**4번 버튼은 오버레이 안이 아니라 HUD 뿌리에 붙는다.** 게임 중에도 눌러야 하기 때문이다.
오버레이보다 나중에 만들어야 어두운 막 위로 올라온다. 버튼을 누른 것이 점프·발사로
세지 않는 것은 각 게임의 `TapInput.OverUI` 가 막아 준다.
자리를 비우려고 점프점프의 Height 와 활쏘기의 점수판을 한 줄씩 내렸다.

### 설정 · 나가기 팝업 (2026-09-08, 4번째 세션)

`@수정사항_02.pdf` 3건. **앱을 나가는 길이 전부 확인 팝업 한 번을 거친다.**
자세한 내용은 `JumpJump/Assets/Shell/README.md` 의 "나가기 / 설정 팝업" 절.

| 어디서 | 누르는 것 | 뜨는 팝업 | [확인] 하면 |
| --- | --- | --- | --- |
| 미니게임 | 오른쪽 맨 위 **←** | 로비로 나가시겠습니까? | 로비로 (저장 안 함) |
| 로비 | 오른쪽 위 **톱니바퀴** | 설정 (빈 판 + [종료] [닫기]) | — |
| 로비 | 설정의 **[종료]** | 종료하시겠습니까? | 앱 종료 |
| 타이틀 · 로비 | 폰 **뒤로 버튼** | 종료하시겠습니까? | 앱 종료 |
| 미니게임 | 폰 **뒤로 버튼** | 로비로 나가시겠습니까? | 로비로 |

- 팝업 한 장 = `Shell/Scripts/PopupPanel.cs`. 씬에 **꺼진 채로 미리 구워져** 있다가 켜진다.
  버튼이 부르는 함수는 전부 `Shell/Scripts/ShellMenu.cs` 에 있고, 씬마다 **필요한 팝업만 연결**되어 있어서
  같은 컴포넌트가 타이틀·로비·미니게임에서 각각 다르게 동작한다.
- **미니게임에 붙이는 방법은 `ShellSceneBuilder.MakeExitToLobbyUI(root)` 한 줄이다.**
  화살표 + 팝업 + ShellMenu 가 통째로 만들어진다. **게임 쪽 코드는 한 줄도 필요 없다.**
  (예전의 `MakeLobbyButton(root, round, hud)` 과 HUD 의 `OnLobbyPressed()` 는 없앴다)
- **팝업이 떠 있는 동안 게임이 멈춘다.** 각 게임 `Update` 의 `Arcade.PopupPanel.Blocking` 검사.
  기획서엔 없지만 고민하는 사이에 죽으면 안 되니까 넣었다. `Blocking` 은 **팝업이 닫힌 프레임까지**
  참이라, [취소]를 누른 그 터치가 점프·발사로 이어지지 않는다.
  **`Step(dt, tap)` 은 팝업을 전혀 모른다** — 그래서 배치 플레이테스트가 그대로 돈다.
- 로비 톱니바퀴 자리는 `ShellSceneBuilder.GearCenter`(0.9268, 0.9496). 09-07 에 그림에서 지웠던
  그 자리를 `Lobby_원본_톱니바퀴포함.png` 에서 재서 넣은 값이다. **그림이 아니라 UI 버튼이다.**
- 팝업 크기 숫자는 전부 `ShellSceneBuilder` 위쪽 상수다 (`PopupWidth` / `AskButtonY` 등).
- 폰의 하드웨어 뒤로 버튼은 Input System 에서 **Esc 키**로 들어온다.
  **윈도우 에디터에서 Esc 를 눌러 확인할 수 있다.** (에디터에서 [확인]은 Play 를 멈춘다)
- 어두운 막은 알파 **0.70**. 이 프로젝트가 Linear 컬러 공간이라 0.55 로는 옅게 보여서 올렸다.

**리소스 폴더가 생겼다.** 사용자가 `@리소스 추가_1차` 폴더에 그림 9장을 넣어 주었고,
`ShellArt` 가 **`@리소스` 로 시작하는 하위 폴더**를 전부 훑는다. 같은 이름이면 **뒤 폴더가 이긴다** —
`@리소스 추가_2차` 에 같은 이름을 넣으면 코드를 고치지 않아도 그쪽이 쓰인다.
프로젝트 안에서는 쓰임새를 알 수 있는 이름으로 바뀐다 (`Popup_01.png` → `Popup_Ask_Exit.png` 등).

### 버튼이 눌리지 않던 버그 (2026-09-08 발견)

**3번째 세션에 붙인 미니게임의 "< LOBBY" 버튼은 눌러도 반응하지 않는 상태였다.**
Unity 에서 UI 버튼이 터치를 받으려면 캔버스에 `GraphicRaycaster` 가 있어야 하는데,
미니게임의 HUD 캔버스는 글자만 그리려고 `new GameObject("HUD", typeof(Canvas), typeof(CanvasScaler))`
로 만들어져서 빠져 있었다. (타이틀·로비의 `MakeCanvas` 에는 들어 있었다)

**배치 모드로는 잡을 수 없는 종류의 버그다** — 캡처는 "보이는지"까지만 확인해 준다.
이제 `MakeExitToLobbyUI` 가 없으면 붙여 주므로 새 미니게임에서도 같은 일이 생기지 않는다.
`TapInput.OverUI` 도 이것이 없으면 항상 false 였으니 같이 고쳐졌다.

---

## 대기 중 / 미결정

**0. 다음 세션에서 가장 먼저 물어볼 것 (2026-09-08 4번째 세션이 여기서 끝났다)**

이번 세션에 붙인 **설정 · 나가기 팝업이 아직 미확인**이다.
그 앞의 세 세션치(활쏘기 / 점프점프 1차 수정 / 화면 수정 4건)는
2026-09-08 에 사용자가 직접 플레이해 보고 **"괜찮다"** 고 확인해 주었다.

**재개하면 먼저 플레이해 봐 달라고 할 것.**
`Tools > Arcade > Build All Scenes` → Play → 톱니바퀴 / 게임 안의 화살표 / **Esc 키**.

물어볼 순서:

- **⓪ 팝업 3건.** 톱니바퀴 자리와 크기, 팝업 판 크기, 버튼 자리.
  숫자는 전부 `ShellSceneBuilder` 위쪽 상수다 (`GearCenter` / `GearWidth` /
  `PopupWidth` / `AskButtonY` / `SettingsButtonY` / `BackButtonWidth`).
  **"< LOBBY" 버튼이 그동안 안 눌렸던 것**도 같이 알려 줄 것 (위 "버튼이 눌리지 않던 버그" 절).
- **① 설정 팝업의 빈 자리.** 사용자가 "추후에 사운드 조절 기능 추가 예정"이라고 적어 두었다.
  **어떤 모습으로 할지 정해 주면 바로 붙일 수 있다.** (음악 / 효과음 슬라이더 두 줄이 흔한 모양)
  다만 **이 앱에는 아직 사운드가 하나도 없다** — 조절할 대상부터 필요하다.
- **② APK 를 다시 구울지.** 작업 폴더의 APK 는 09-07 것이라 팝업이 안 들어 있다.
  폰에서 확인하려면 `Tools > JumpJump > Build Android APK` (10분 이상, 백그라운드로).
  폰에서 볼 때 **하드웨어 뒤로 버튼**과 **20:9 화면의 로비 위아래 띠**를 물어볼 것.
- **③ 활쏘기 난이도.** "테이블이 추가될 예정"이라고 했으니 새 `archery.csv` 를 가져왔는지 물어볼 것.
- **④ 남은 그림.** 활쏘기 **배경(하늘)** 은 아직 내가 만든 그라데이션이다.
  점프점프 배경 구름도 사실상 한 종류다 (아래 2번 항목).
- **⑤ 타이틀 제목.** 더 키우려면 **두 줄짜리 제목 그림**이 필요하다 (위 "화면 수정" 절).
  09-08 에는 "괜찮다"고 했으므로 **먼저 캐묻지 말 것.**

1. **난이도 확인** — 두 게임 다 CSV 구간표로 굴러가는데, 사용자가 아직
   "손맛이 맞는지"를 명시적으로 말한 적이 없다. 점프점프는 `config.csv`, 활쏘기는 `archery.csv`.
1-1. **껍데기(타이틀/로비) 관련 미결정** — 2026-09-06에 붙이면서 남긴 것들:
   - **한글 폰트를 프로젝트에 넣지 않았다.** 지금은 Unity 기본 LegacyRuntime 폰트가
     OS 폰트로 대체해서 그린다. 윈도우 에디터 캡처에서는 정상이지만
     **안드로이드 실기기에서 한글이 나오는지 확인이 필요하다.** 깨지면 무료 폰트
     (나눔고딕 등)를 넣고 `ShellUI.AddText` 의 폰트를 바꾸면 된다.
     다만 2026-09-07 에 타이틀 이름과 로비 이름표가 **그림으로 바뀌면서 위험이 사라졌다.**
     **지금은 화면에 한글을 글자로 그리는 곳이 한 군데도 없다.**
     (미니게임 HUD 는 전부 영어·숫자, 이름표는 두 게임 다 그림)
     그래도 게임을 추가하면서 이름표 그림 없이 `displayName` 만 넣으면 다시 한글이 글자로 찍힌다.
   - **로비 그림이 9:16 이라 요즘 폰(20:9)에서는 위아래에 어두운 띠가 생긴다.**
     칸이 잘리면 안 되므로 일부러 Contain 으로 넣었다. 없애려면 세로로 더 긴
     로비 그림이 필요하다. (`00_lobby_tall.png` 미리보기로 확인 가능)
   - **로비 그림의 칸 번호가 1,2,3 / 4,5,6 / 8,9,10 으로 7이 빠져 있다.** 그림 쪽 오타.
   - **설정 버튼은 2026-09-08 에 UI 버튼으로 다시 얹었다.** (09-07 에 그림에서 지웠던 자리 그대로)
     설정 팝업 안은 아직 [종료] [닫기] 두 개뿐이고, 위쪽은 사운드 조절 자리로 비워 두었다.
   - **앱 이름(폰 홈 화면 라벨)을 "심심할 때 하는 게임" 으로 바꿨다.**
     패키지 이름은 `com.zkfpf.jumpjump` 그대로다 — 바꾸면 기존 설치본과 별개 앱이 된다.
2. **BG 지면 띠 반복 문제 — 2026-09-07 해결됨.** 높이별 배경 3장으로 갈아엎으면서 사라졌다.
   대신 남은 것은 **배경 그림의 완성도**다. 지금 타일은 원본 가로형 3장(896x400)에서
   기계적으로 뽑아낸 것이라 요소가 적다:
   - 평야의 구름이 사실상 **한 종류를 변형**한 것 (원본에 잘리지 않은 구름이 1개뿐이었다)
   - 산은 봉우리 몇 개가 200~500m 내내 반복
   - 세 장의 하늘색 차이가 커서 전환이 눈에 띌 수 있다
   **세로형(896x1400, 위끝=아래끝)으로 새로 뽑은 그림을 받으면 그대로 좋아진다.**
   조건은 `배경이미지_준비가이드.html`, 다시 뽑는 스크립트는 `BG_시험\배경타일_만들기.py`.
   사용자가 새 그림을 가져오면 이 순서로 처리할 것.
3. **git 저장소 — 아직 안 만들었다.**
   2026-09-06 세션 끝에 물었더니 **"다음에 할게요"**, 그 뒤로도 여러 번 언급했으나
   **사용자가 답하지 않고 작업을 진행시켰다.** 거절은 아니지만 관심이 낮은 것으로 보인다.
   **먼저 캐묻지 말 것.** 큰 수정을 시작하기 직전에 한 줄로만 가볍게 얹고 넘어가면 된다.
   (미니게임 2개가 다 검증까지 끝난 지금이 첫 커밋으로는 좋은 지점이긴 하다.
    특히 앞으로 그림을 계속 갈아 끼울 텐데, 지금 커밋해 두면 "그림 바꾸기 전으로 돌려줘"가 된다)
   만들 때는 `D:\00.JumpJump` 에 저장소를 만들고 Unity 용 `.gitignore`
   (Library / Temp / obj / Logs / *.csproj / *.sln 제외)를 같이 넣는다.
   설정·커밋은 내가 대신 하되, 나중에 사용자가 스스로 "3일 전으로 돌려줘"라고
   요청할 수 있도록 개념은 설명해 줄 것.

---

## 아직 없는 것

사운드 / 파티클 / 아이템 / 특수 발판 / 캐릭터 선택 화면 / 랭킹.
**사운드가 하나도 없다** — 설정 팝업의 빈 자리에 사운드 조절을 넣으려면 그것부터 필요하다.
HUD 폰트는 Unity 기본 LegacyRuntime (TMP Essential Resources 미임포트).
껍데기 쪽은 **설정 팝업 안의 실제 설정 항목**과 로비의 최고 점수 표시가 없다.
(설정 팝업 자체와 안드로이드 뒤로가기는 2026-09-08 에 붙였다)
활쏘기 쪽은 맞은 화살이 과녁에 꽂혀 남는 연출 / 바람 같은 방해 요소가 없다.
과녁·활·화살은 사용자가 넣은 진짜 그림이지만 **배경(하늘)만 아직 코드로 만든 그라데이션이다.**
**미니게임은 점프점프 · 활쏘기 2개이고, 나머지 7칸은 잠김 상태다.**

---

## 작업 규칙

- **난이도 수치는 CSV, 나머지 튜닝 값은 Config 에셋이다. 코드에 숫자를 박지 말 것.**
  새 튜닝 값이 필요하면 Config 에셋에 필드를 추가한다.

  | 게임 | 난이도 CSV (작업 폴더) | 그 밖의 튜닝 값 |
  | --- | --- | --- |
  | 점프점프 | `config.csv` (발판 폭 / 타이머 / 속도) | `Assets/JumpJump/GameConfig.asset` |
  | 활쏘기 | `archery.csv` (과녁 속도 / 축소율 / 화살 수) | `Assets/Archery/ArcheryConfig.asset` |

- **미니게임을 새로 만들 때도 `Step(dt, tap)` 구조를 지킬 것.** 게임 로직이 입력과
  델타타임을 인자로 받아야 배치 모드 플레이테스트·미리보기를 만들 수 있다.
  이 프로젝트의 검증 방식이 통째로 거기에 기대고 있다.
- **씬을 손으로 편집하지 말 것.** 빌더가 씬을 통째로 굽는 방식이라,
  씬에 직접 넣은 변경은 다음 빌드에서 전부 사라진다. 씬 구성을 바꾸려면
  점프점프는 `JumpJumpSceneBuilder.Populate()`, 활쏘기는 `ArcherySceneBuilder.Populate()`,
  타이틀/로비는 `ShellSceneBuilder.PopulateTitle()` / `PopulateLobby()` 를 고쳐야 한다.
- **미니게임을 추가할 때 코드를 새로 쓰지 말 것.** 씬 + PNG 두 장(아이콘·이름표) +
  `GameCatalog.asset` 한 줄이면 끝나도록 만들어 두었다. 방법은 `Assets/Shell/README.md`.
  **이름표 그림에는 게임 이름이 그려져 있어야 하고, 가로:세로 약 2.15:1 이어야 한다.**
  로비 이름표는 게임마다 크기가 같아야 해서 **고정 칸 크기로 그려 넣기 때문**이다.
  이름표가 없으면 빈 이름표(`Shell/Art/NamePlate_Blank.png`) 위에 `displayName` 이 글자로 찍힌다.
  (다만 새 씬은 `ShellSceneBuilder` 의 씬 목록과 `JumpJumpBuild.Scenes` 에도 넣어야 한다)
- **미니게임의 나가기 버튼·팝업은 게임마다 만들지 말 것.**
  `Arcade.EditorTools.ShellSceneBuilder.MakeExitToLobbyUI(root)` 한 줄이면
  화살표 + "로비로 나가시겠습니까?" 팝업 + ShellMenu 가 통째로 만들어진다.
  **게임 쪽 코드는 한 줄도 필요 없다.** 그래야 어느 게임에서나 자리·모양이 같다.
  단 게임의 `Update` 에 `if (Arcade.PopupPanel.Blocking) return;` 한 줄은 넣어야
  팝업이 떠 있는 동안 게임이 멈춘다.
- **타이틀/로비의 UI 위치는 픽셀이 아니라 "배경 그림 안의 비율" 이다.**
  (딱 하나, 타이틀의 제목 그림만 **화면** 기준이다 — 배경이 잘려도 제목은 안 잘리도록)
  `AspectFitter` 가 배경을 맞추고 나머지는 앵커로 따라붙는다. 위치를 조정할 일이 생기면
  `TitleScreen` / `LobbyScreen` 인스펙터의 비율 값을 고칠 것. 코드 상수가 아니다.
- **물리 엔진을 쓰지 않는다.** Rigidbody2D / Collider2D 없이 직접 적분하고,
  착지는 `PlatformManager.FindLanding()` 이 "이번 프레임에 발이 발판 윗면을
  아래로 통과했는가"를 수식으로 판정한다. 콜라이더 기반 기믹을 추가하려면
  이 판정 함수를 거치도록 설계할 것.
- **껍데기 그림(`@Title.jpg` / `@Title_Text.png` / `@Lobby.png` / 버튼 / 아이콘 / 이름표)을
  교체하면 파일을 덮어쓰기만 하면 된다.** 작업 폴더 쪽이 더 새로우면 Build All Scenes 가
  자동으로 가져오면서 투명 여백까지 잘라 낸다. 날짜가 같아 안 따라오면
  **Tools > Arcade > Import Shell Art** 로 강제로 다시 가져올 수 있다.
- 아트 PNG 를 교체하면 **Tools > JumpJump > Setup Art Assets** 를 다시 실행해야
  PPU·피벗이 재계산된다 (`userData` 마커로 중복 실행을 건너뛰기 때문).
- 사용자는 기획·데이터 설계는 능숙하지만 git·마크다운·Claude Code 는 배우는 중이다.
  도구 이야기는 명령어 나열보다 "무엇을 하는 물건인지"부터 설명하고,
  파일 작성·커밋은 대신 해 주되 개념은 이해할 수 있게 알려줄 것.
