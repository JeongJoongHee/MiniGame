# Unity 임시 및 빌드 생성 폴더 제외
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
MemoryCaptures/

# 자동 생성되는 Visual Studio/IDE 파일 제외
*.csproj
*.unityproj
*.sln
.vs/
# 글꼴 원본의 압축본은 저장소에 넣지 않는다 (같은 글꼴의 .ttf/.otf 만 있으면 된다)
Font/*.zip
Font/*.tar.gz

# 빌드 결과물은 저장소에 넣지 않는다 (필요하면 다시 구우면 된다)
*.apk
*.aab
/JumpJump.zip

# Unity 안드로이드 빌드가 쓰는 임시 폴더 (빌드할 때마다 바뀜)
.utmp/
